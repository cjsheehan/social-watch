export function Tweet(userName, text, hashTags, createdAt, location, idStr) {
	this.userName = userName;
	this.text = text;
	this.hashTags = hashTags;
	this.createdAt = createdAt;
	this.location = location;
	this.idStr = idStr;
}
